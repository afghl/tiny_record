require 'spec_helper'

describe TinyRecord, "database engine" do
  before do
    class Post < TinyRecord::Base; end
    class User < TinyRecord::Base; end
  end

  it "do something" do
    
  end
end
